{
  "entities": {
    "User": {
      "title": "User / Driver Profile",
      "description": "User account schema including NFC authentication field",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Unique user/driver identifier"
        },
        "name": {
          "type": "string",
          "description": "Full name of the driver"
        },
        "email": {
          "type": "string",
          "description": "Email address of the driver"
        },
        "role": {
          "type": "string",
          "description": "User role: driver or admin"
        },
        "nfc_tag_id": {
          "type": "string",
          "description": "Captured NFC Card unique hardware identifier (UID) for taps"
        },
        "fileNumber": {
          "type": "string",
          "description": "Company file ID used for legacy credentials lookup"
        },
        "password": {
          "type": "string",
          "description": "Secure password PIN used for hybrid standard login"
        }
      },
      "required": ["uid", "name", "role"]
    },
    "AttendanceRecord": {
      "title": "Attendance Punch Record",
      "description": "Punch-in and punch-out timing logs mapped to driver status",
      "type": "object",
      "properties": {
        "logId": {
          "type": "string",
          "description": "Unique attendance punch collection ID"
        },
        "uid": {
          "type": "string",
          "description": "Referencing driver unique user/driver ID"
        },
        "date": {
          "type": "string",
          "description": "Local ISO-8601 representation of date (YYYY-MM-DD)"
        },
        "checkIn": {
          "type": "string",
          "description": "Check-in time stamp string"
        },
        "checkOut": {
          "type": "string",
          "description": "Check-out time stamp string"
        },
        "punchMethod": {
          "type": "string",
          "description": "Method of attendance punch registered: 'NFC' or 'Manual'"
        }
      },
      "required": ["logId", "uid", "date", "checkIn", "punchMethod"]
    },
    "DailyAttendance": {
      "title": "Daily Attendance Metrics",
      "description": "Odometer and duration tracking metrics used for dashboard calculations",
      "type": "object",
      "properties": {
        "date": { "type": "string" },
        "startOdometer": { "type": "integer" },
        "endOdometer": { "type": "integer" },
        "startTime": { "type": "string" },
        "endTime": { "type": "string" },
        "totalDurationMinutes": { "type": "integer" }
      }
    },
    "FuelLog": {
      "title": "Fuel Log Entry",
      "description": "Log of liters consumed, amount spent, and odometer at fuel check",
      "type": "object",
      "properties": {
        "date": { "type": "string" },
        "fuelOdometer": { "type": "integer" },
        "liters": { "type": "number" },
        "amount": { "type": "number" }
      }
    },
    "ServiceLog": {
      "title": "Service Log Entry",
      "description": "Log of service vehicle details and odometer at maintenance check",
      "type": "object",
      "properties": {
        "date": { "type": "string" },
        "serviceOdometer": { "type": "integer" },
        "serviceDetails": { "type": "string" }
      }
    }
  },
  "firestore": {
    "Companies/{companyId}": {
      "description": "Registered company tenants loaded by the multi-tenant engine"
    },
    "Companies/{companyId}/users/{uid}": {
      "schema": "User",
      "description": "Active isolated users belonging specifically to this company tenant"
    },
    "Companies/{companyId}/Daily_Attendance/{dateDoc}": {
      "schema": "DailyAttendance",
      "description": "Isolated true kilometer and duration metrics for daily dashboard reporting"
    },
    "Companies/{companyId}/Fuel_Log/{logId}": {
      "schema": "FuelLog",
      "description": "Isolated fuel log entries tracing liters consumed and purchase values"
    },
    "Companies/{companyId}/Service_Log/{logId}": {
      "schema": "ServiceLog",
      "description": "Isolated service and maintenance details logs"
    }
  }
}
